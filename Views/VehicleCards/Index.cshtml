@model IEnumerable<CEPiK.Models.VehicleCard>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Number)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Series)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ExpirationData)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Series)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExpirationData)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VehicleCardID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VehicleCardID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VehicleCardID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
